apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.namespaces }}
spec:
  clusterIP: {{ .Values.headless.clusterIP }}
  selector:
    app: {{ .Values.headless.app }}
  ports:
    - port: {{ .Values.headless.port }}
      targetPort: {{ .Values.headless.port }}
